# 应用环境变量示例
# 设置为 PostgreSQL 连接字符串
# 本地开发可使用: postgresql://postgres:postgres@localhost:5432/postgres
# docker-compose 会自动提供: postgresql://postgres:postgres@db:5432/appdb
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres

# Next.js 运行端口（可选）
PORT=3000
AUTH_SECRET=change-me

# 第三方 OpenAI API 协议配置
# 例如 OpenAI 官方: https://api.openai.com
# 也可替换为兼容协议的其他服务地址
AI_BASE_URL=https://api.openai.com
AI_API_KEY=sk-xxxx
AI_MODEL=gpt-4o-mini

# 提示词裂变配置
# 裂变条数
PROMPT_FISSION_COUNT=5
# 模板中可用变量: {{prompt}}, {{count}}
PROMPT_FISSION_TEMPLATE="请基于以下提示词进行提示词裂变，生成 {{count}} 条高质量、风格多样且目标明确的中文提示词。要求：仅返回 JSON 数组（字符串），不要附加任何解释。原始提示词：{{prompt}}"
